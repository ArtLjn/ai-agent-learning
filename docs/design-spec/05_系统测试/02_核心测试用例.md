# 核心测试用例

> **v2.0 修订（2026-07-01）**：本文档保留 v1.x 测试用例编号 TC-001~TC-808。v2.0 新增测试用例编号规则与前缀见 [01_测试策略.md](./01_测试策略.md) 第 12 章（TC-RAG-* / TC-CLIENT-* / TC-TRACE-* / TC-PROMPT-* / TC-TOKEN-*）。

## 1. 工单创建

| 编号 | 输入 | 预期结果 |
| --- | --- | --- |
| TC-001 | 提交有效工单内容 | 返回 `ticket_id` 和 `received` 状态 |
| TC-002 | 批量提交两条工单 | 返回两个独立结果 |
| TC-003 | 查询已存在工单 | 返回完整工单详情 |
| TC-004 | 查询不存在工单 | 返回 404 |
| TC-005 | 提交自然语言长描述 | TicketIntentAgent 提取分类、优先级、影响范围并格式化正文 |

## 2. 分类与路由

| 编号 | 输入 | 预期结果 |
| --- | --- | --- |
| TC-101 | “系统崩溃无法登录” | 分类为技术类，进入处理流程 |
| TC-102 | “账单重复扣费” | 分类为账务类，进入处理流程 |
| TC-103 | “我要投诉服务态度” | 分类为投诉，进入升级流程 |
| TC-104 | “核心业务完全不可用，全部用户无法访问” | 优先级为 P0，进入升级并转人工审核 |

## 3. 审核与重试

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-201 | 审核评分高于阈值 | 工单进入通知并完成 |
| TC-202 | 审核评分低于阈值且未超限 | 返回处理节点重试 |
| TC-203 | 重试次数达到上限 | 工单进入 `pending_human_review`，生成 pending 审核单 |
| TC-204 | 业务操作缺少订单号或支付凭证 | 风险策略标记缺失字段，工单转入人工审核或建议请求补充 |

## 4. 知识库

| 编号 | 输入 | 预期结果 |
| --- | --- | --- |
| TC-301 | 上传标题和正文完整的文档 | 返回 `chunks_added` |
| TC-302 | 缺少标题 | 返回 400 |
| TC-303 | 缺少正文 | 返回 400 |
| TC-304 | Qdrant 不可用 | 返回知识库服务不可用 |

## 5. WebSocket

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-401 | 连接单工单 WebSocket | 接收该工单节点更新 |
| TC-402 | 连接全局监控 WebSocket | 接收所有工单节点更新 |
| TC-403 | 客户端断开连接 | 后端清理连接列表 |

## 6. Trace

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-501 | 查询已处理工单 trace | 返回 trace 和 span 树 |
| TC-502 | 查询不存在 trace | 返回 404 |
| TC-503 | 查询 trace 统计 | 返回节点耗时统计 |
| TC-504 | 查询 trace 决策点 | 返回 classify/review/retry_check 等决策语义 |

## 7. 统计分析

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-601 | 查询统计接口 | 返回分类分布、优先级分布、处理统计 |
| TC-602 | 提交满意度反馈 | 反馈记录写入并影响满意度统计 |

## 8. 人工审核（Phase 5 补充）

### 8.1 四种触发场景

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-701 | 投诉类工单 escalate | 进入 `human_review_wait`，工单状态 `pending_human_review`，队列可见 |
| TC-702 | AI 审核失败 retry_count=3 | 进入 `human_review_wait`，trigger_type=`review_failed` |
| TC-703 | 工作流执行抛异常 | 兜底为 `error_fallback` 类型 pending 审核单 |
| TC-704 | completed 工单提交 satisfied=false | 自动创建 `user_request` 类型 pending 审核单，工单状态回退 |

### 8.2 五种决策路由

| 编号 | 输入决策 | 预期路由 |
| --- | --- | --- |
| TC-711 | `approve` | 沿用原 processing_result → notify → complete |
| TC-712 | `rewrite`（含 rewritten_result） | 覆盖 processing_result → notify → complete |
| TC-713 | `reprocess` | 清空 processing_result + retry_count → process → review |
| TC-714 | `reject` | 标记为已驳回 → complete |
| TC-715 | `request_info` | 审核单置为 decided，工单进入 `waiting_user_input`，写入审核员补充消息 |

### 8.3 队列、详情、统计接口

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-721 | `GET /reviews/queue` 支持按 trigger_type/category/priority 筛选 | 队列按优先级 + 等待时长排序 |
| TC-722 | `GET /reviews/{ticket_id}` 返回工单上下文 + 当前 + 历史审核 | 含 trace 摘要、AI 建议 |
| TC-723 | `GET /reviews/stats` 计算 ai_adoption_rate | 分子=采纳数，分母=有 AI 建议的决策数 |
| TC-724 | 决策已 decided 的工单再次提交 | 返回 409 |
| TC-725 | `POST /reviews/{ticket_id}/decision` 提交 `request_info` | 返回 `next_node=waiting_user_input`，`workflow_resumed=false` |

### 8.4 WebSocket 事件 / Trace 集成

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-731 | 工单进入待审核时连接 `/ws/monitor` | 收到 `review_requested` 事件 |
| TC-732 | 审核员提交决策 | 收到 `review_decided` 事件（含 next_node） |
| TC-733 | `GET /tickets/{id}/trace` | span 树含 `span_type=human_decision` 的 decided span |
| TC-734 | 审核员请求用户补充 | 收到 `user_input_requested` 事件和普通 `ticket_update` |

### 8.5 端到端流程（tests/e2e/test_human_review_flows.py）

| 编号 | 流程 |
| --- | --- |
| TC-741 | 投诉 → escalate → human_review_wait → approve → complete |
| TC-742 | review 失败 3 次 → human_review_wait → rewrite → complete |
| TC-743 | review 失败 3 次 → human_review_wait → reprocess → process → review → 通过 |
| TC-744 | 工作流异常 → error_fallback → approve → complete |
| TC-745 | completed + satisfied=false → user_request 审核 |

## 9. 用户补充信息（v1.3 补充）

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| TC-801 | 查询不存在工单的沟通记录 | `GET /tickets/{id}/messages` 返回 404 |
| TC-802 | 工单处于 `waiting_user_input`，用户提交补充消息 | 写入 `ticket_messages`，随后调用 `resume_from_user_input` |
| TC-803 | 工单不处于 `waiting_user_input`，用户提交补充消息 | 返回 409，不写入消息 |
| TC-804 | 用户提交空白补充消息 | Pydantic 校验失败，返回 422 |
| TC-805 | 读取某工单沟通记录 | 按 `created_at` 升序返回审核员请求和用户回复 |
| TC-806 | 用户补充后恢复处理 | 同一 trace 中出现 `user_input_resume`、`process`、`review` span |
| TC-807 | 用户补充后 ReviewerAgent 评分通过 | 工单进入 `completed` |
| TC-808 | 用户补充后 ReviewerAgent 评分仍不通过 | 按原重试和人工审核策略继续流转 |

## 相关文档

- [01_测试策略.md](./01_测试策略.md) — 测试策略与 v2.0 用例编号前缀规则
- [11_RAG服务独立项目设计.md](../01_正式设计/11_RAG服务独立项目设计.md) — RAG 服务设计（TC-RAG-*）
- [12_Token控制台.md](../01_正式设计/12_Token控制台.md) — Token 控制台（TC-TOKEN-*）
- [13_开发人员工作台.md](../01_正式设计/13_开发人员工作台.md) — 开发人员工作台（TC-TRACE-* / TC-PROMPT-*）
